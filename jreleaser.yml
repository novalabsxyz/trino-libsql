project:
  name: trino-libsql
  description: Trino connector for libSQL/Turso databases
  license: Apache-2.0
  links:
    homepage: https://github.com/novalabsxyz/trino-libsql

release:
  github:
    overwrite: true
    changelog:
      formatted: ALWAYS
      preset: conventional-commits

distributions:
  trino-libsql:
    type: JAVA_BINARY
    artifacts:
      - path: target/trino-libsql-{{projectVersion}}.zip
